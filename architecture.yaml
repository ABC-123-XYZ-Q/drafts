elements:
  - id: user
    name: User
    role: actor
    description: User should not trigger change again
    relationships:
      - target: system
        description: Uses
  - id: system
    name: Bingbing
    role: system
    description: This is a test project
specVersion: '0.1'
